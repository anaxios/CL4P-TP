---
version: "3.5"
services:
  # postgres:
  #   image: postgres:15
  #   restart: unless-stopped
  #   env_file:
  #     - .env
  #   ports:
  #     - 5432:5432
  #   volumes:
  #     - ./db/sql_schema.sh:/docker-entrypoint-initdb.d/sql_schema.sh

  # adminer:
  #   image: adminer
  #   restart: always
  #   ports:
  #     - 8080:8080

  # chromadb:
  #   image: chromadb/chroma
  #   container_name: chromadb
  #   restart: unless-stopped
  #   ports:
  #     - 8000:8000
  #   env_file:
  #     - .env
  #   volumes:
  #     - ./chromadb/:/chroma/chroma

  cl4p-tp:
    #image: ghcr.io/anaxios/cl4p-tp:latest
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./LOG:/app/LOG
    depends_on:
      - postgres
      # - chromadb
